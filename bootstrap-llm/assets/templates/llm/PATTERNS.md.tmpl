<!-- The how-to cookbook, from Phase 2 research (+ existing code when present). Each pattern:
     name, when to use, a SHORT current-version example. This file saves every future session
     from re-deriving (or hallucinating outdated) idioms. Delete comments. -->
# Code Patterns

Recipes for recurring tasks in this stack. Follow these before inventing your own; extend this file when you establish a new pattern (same change, per `rules/llm-docs.md`).

{{PATTERNS}}
<!-- 4-8 patterns matched to the stack, each ~10-25 lines:

## Add a <route/endpoint/screen>

When: ...
```<lang>
<minimal current-version example, compilable, project-flavored (real paths)>
```
Notes: the 1-2 mistakes models/newcomers make here (from research).

Typical set, web app: route/page, data access, mutation/action, component, test, config/env.
Typical set, API: endpoint+schema, service layer, DB access/migration, error response, test, background job.
-->

---
Researched: {{RESEARCH_DATE}} | Stack: {{STACK_VERSIONS}} | Sources: {{SOURCES}}
Stale if: major version bump, or older than ~3 months. Refresh: /bootstrap-llm --refresh
