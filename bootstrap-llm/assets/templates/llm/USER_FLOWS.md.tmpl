<!-- From the interview's 3-7 major user stories. Future tickets reference flows by number;
     sessions update Status as flows get built (see rules/llm-docs.md). Delete comments. -->
# User Flows

The product's major flows. A ticket usually implements or modifies a slice of one of these; read the relevant flow before starting so the slice fits the whole.

Status: `planned` | `partial` | `built`

{{FLOWS}}
<!-- One section per story:

## Flow 1: <name>                      [planned]

**Actor**: <user type from CONTEXT.md>  **Trigger**: <what starts it>

1. <step>
2. <step>
3. <outcome>

**Notes**: edge cases or constraints the interview surfaced. Omit if none.
-->
