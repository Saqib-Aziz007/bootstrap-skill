<!-- FULL DEPTH: style + error handling only (testing.md and security.md are separate).
     Fill from Phase 2 research + detection; version-specific; mechanical tells. ≤ 4 KB. -->
# Coding Style

Hard rules for all code in this repo. If you cannot tell whether code violates a rule, it does not belong here.

## {{STACK_SHORT}} rules

{{STYLE_RULES}}
<!-- 8-14 researched, version-specific hard rules with mechanical tells. Group by topic
     (components/modules, typing, imports, naming, logging) if longer than ~8 bullets. -->

## Error handling

{{ERROR_RULES}}
<!-- How errors are raised, wrapped, logged in this stack; what context they carry;
     what the user-facing failure path looks like. -->

---
Researched: {{RESEARCH_DATE}} | Stack: {{STACK_VERSIONS}} | Sources: {{SOURCES}}
Stale if: major version bump, or older than ~3 months. Refresh: /bootstrap-llm --refresh
